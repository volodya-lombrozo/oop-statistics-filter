# How to run sourcemeter:
```shell
docker run -v "$(pwd):/workdir" sourcemeter-app 
```

or the full path of the deployed docker image:

```shell
docker run -v "$(pwd):/workdir" lombrozo/sourcemeter-app:latest
```